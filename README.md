<div align="center">
<img width="1200" height="630" alt="Remigiusz Bednarczyk" src="https://remigiuszbednarczyk.com/img/og-image.png" />
</div>

# Remigiusz Bednarczyk | Quality Engineering Portfolio

Personal website of a **Quality Engineering Lead / Test Manager** working in **GxP-regulated environments, test strategy, and AI-assisted testing**.

**Live:** [remigiuszbednarczyk.com](https://remigiuszbednarczyk.com/)

[![CI/CD Pipeline](https://github.com/rembednarczyk/remigiuszbednarczyk.com/actions/workflows/ci.yml/badge.svg)](https://github.com/rembednarczyk/remigiuszbednarczyk.com/actions/workflows/ci.yml)

Using: [![React](https://img.shields.io/badge/React-19-61DAFB?logo=react&logoColor=black)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-6-3178C6?logo=typescript&logoColor=white)](https://www.typescriptlang.org/)
[![Vite](https://img.shields.io/badge/Vite-8-646CFF?logo=vite&logoColor=white)](https://vite.dev/)
[![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4-06B6D4?logo=tailwindcss&logoColor=white)](https://tailwindcss.com/)

Lighthouse score, desktop preset, re-measured on every build by `npm run check:lighthouse`: [![Performance](https://img.shields.io/badge/Performance-100-00CC66?logo=lighthouse&logoColor=white)](https://remigiuszbednarczyk.com/) [![Accessibility](https://img.shields.io/badge/Accessibility-100-00CC66?logo=lighthouse&logoColor=white)](https://remigiuszbednarczyk.com/) [![Best Practices](https://img.shields.io/badge/Best%20Practices-100-00CC66?logo=lighthouse&logoColor=white)](https://remigiuszbednarczyk.com/) [![SEO](https://img.shields.io/badge/SEO-100-00CC66?logo=lighthouse&logoColor=white)](https://remigiuszbednarczyk.com/)

---

## Scope and Intent

A production-grade frontend project that demonstrates how I approach:

- Quality treated as a system that runs on every commit
- Performance-first architecture
- Accessibility and UX consistency
- SEO and **LLM-ready content structure**

> **Disclaimer:** This project is deliberately over-engineered for a portfolio website. Strict component decomposition, domain logic separation, CI/CD quality gates, and automated testing are here to show how I work on software quality, maintainability, and engineering standards in enterprise environments.

---

## Quality-Driven Approach

Engineering discipline applied to a personal project:

- Semantic HTML and structured content
- Accessibility practices covering focus management, keyboard navigation, and contrast
- Print-ready layout (A4 optimized via `@media print`)
- SEO with Open Graph and JSON-LD
- LLM-friendly structure (`llm.txt`, structured sections)
- Print-ready CV template with certifications and credentials tracking
- Analytics gated behind explicit consent (Google Consent Mode v2)

---

## Architecture and Key Decisions

### Data-Driven Design

All content lives outside the UI, and outside the code: every word the site states is JSON in `src/content/`, one file per thing it says. Two modules assemble it into the typed shapes components read — `portfolioFacts.ts`, which carries no JSX so the Vite config can load it to write the JSON-LD and `llm.txt`, and `portfolioData.tsx`, which draws the cards. Sections read from those; nothing writes back. Content changes without touching a component or a language, which is the point: a paragraph is editable by something that is not this repository. The print template renders the same entries the page does. What stays in code is what is not a sentence — a computation, a name from a fixed set, a contact detail kept in parts — and `tests/contentBoundary.test.ts` holds that line rather than trusting it.

`src/utils/domain.ts` holds pure helpers that run without React. `getYearsOfExperience` feeds the hero: the years figure is computed from a career start date on every page load, in both the headline metric and the description sentence, so it rolls over on its own instead of sitting there as a number someone has to remember to bump.

`src/lib/` holds logic that has no React in it at all. `particleField.ts` is the canvas simulation, handed a drawing context and a size; `contactForm.ts` is the contact form's transport, which reads the fields, builds the body and reports which of two things happened. Both used to live inside the components that render them, where the only way to reach them was to mount the component.

---

### Component Decomposition and Custom Hooks

Large sections are decomposed into focused components such as `ExperienceItem`, `ProjectCard`, and `SkillCategoryCard`.
Custom hooks (`useActiveSection`, `useScrollToSection`, `useHashTarget`, `useAutoPrint`, `useModalA11y`, `useScrollLock`, `useContactForm`, `useCookieConsent`) hold side effects and DOM work, which keeps the components declarative.

---

### Software Engineering Practices

- **Single Responsibility Principle:** `ExperienceSection` handles layout and iteration; `ExperienceItem` renders one job entry.
- **Separation of Concerns:** content (`src/content/`), the modules that shape it (`data/`), pure helpers (`utils/domain.ts`), React-free logic (`lib/`), side effects (`hooks/`), and UI (`components/`) stay isolated.
- **DRY:** shared UI lives in `ui/` — the leaf elements (`SectionHeading`, `Button`, `Tag`) and the shells the page is assembled from (`PageSection`, `Reveal`, `IconCard`, `IconListItem`, `Modal`).
- **Strong typing:** TypeScript runs in `strict` mode with `@types/react` installed, so component props, hooks, and event handlers are all checked. Shared contracts live in `src/types/index.ts`.

---

### Stack

- React 19 with TypeScript 6
- Vite 8
- Tailwind CSS 4
- Motion (Framer Motion)
- Lucide icons
- Vitest with React Testing Library
- Storybook with the accessibility addon and test runner

Chosen for build speed, maintainability, and developer experience.

---

### UX and Interaction

- Scroll spy that maps sub-sections to their parent navigation entry
- Smooth navigation between sections
- Animations restricted to `transform` and `opacity`, which keeps them off the layout path
- Focus trapped inside open dialogs, and returned to the trigger on close
- `prefers-reduced-motion` honoured across the whole page, not only the animated background: `MotionProvider` sets `reducedMotion="user"`, which drops the transform from every entrance and leaves the fade alone. Measured on the built page: a section arriving holds two positions instead of sliding through twenty-odd

---

### Performance Optimization

- Single bundle by design. `React.lazy` was tried and removed because the extra requests hurt LCP on HTTP/1.1 more than the smaller entry chunk helped. The work went into making that one bundle smaller instead.
- Motion is set up once at the root by `MotionProvider`, which loads one feature set, `domAnimation`, through `LazyMotion`; every animated element uses `m` rather than `motion`. The page animates with `initial`, `animate`, `exit`, `transition`, `whileInView` and `viewport`, and nothing else, so the drag, layout and gesture code never ships. `strict` mode makes a stray `motion` element throw rather than quietly restore the full bundle.
- The printed CV's QR code is drawn from committed path data. It encodes a constant, and generating it in the browser meant shipping a QR library to every visitor for a picture that only appears on paper.
- Passive scroll listeners so scrolling never waits on a handler
- Canvas background using a spatial hash for particle linking and batched path strokes, with the backing store scaled to `devicePixelRatio`
- No connection to the analytics origin until the visitor accepts. There was a preconnect here, and a tag beside it, both firing before anyone had answered the banner; the privacy policy said no measurement data was collected until acceptance, and that could not both be true.
- ViteSingleFile was considered and rejected to avoid over-optimizing at the cost of future scalability

---

## Development Guidelines and Guardrails

Development follows two documents, both of which apply to human and AI contributors:

- [Ways of Working](docs/guidelines/WAYS_OF_WORKING.md) governs how changes are made: the operating loop of a change, how the project's memory is structured, what a test is for, which defect classes to aim at, how much process a change deserves, and what has to be recorded before a change counts as done. It is project-agnostic and takes precedence. Two of its rules decide most of what is in this repository: *a rule you cannot enforce automatically is a wish*, and the tier of a change is set by whether its defect could be seen at all without a gate — not only by what a silent error would cost. This project has no money, auth or data integrity on its critical path, and that second axis is why it has a gate for the printed CV's page breaks and one for what a keyboard focus ring paints.
- [AI Instructions](docs/guidelines/AI_INSTRUCTIONS.md) covers this repository specifically: execution protocol, architecture rules, Lighthouse guardrails, and UI/UX conventions.
- [CLAUDE.md](CLAUDE.md) is the working agreement read at the start of a session: authorship, the branch and pull request flow, and the habits that catch a wrong measurement. It restates neither of the above, and points at both.

`.claude/hooks/session-start.sh` runs at session start and sets the repository's git identity, because the container's global one is not the owner's and a rule that depends on remembering is not a rule. `.claude/hooks/no-shell-edits.sh` refuses the two things CLAUDE.md names and that carried on happening anyway: changing a repository file through a redirect, a heredoc or `sed -i`, and discarding uncommitted work with `git checkout <path>`. Reading and searching through the shell are untouched. **It is not registered**, so it does not run: a `PreToolUse` entry edits the agent's own permission surface and the permission classifier refuses to write one, so the owner adds it by hand. Until that entry exists the rule is held by discipline alone, which is what `tests/hookRegistration.test.ts` makes the documents say out loud. To turn it on, add to `.claude/settings.json` a `PreToolUse` array whose one entry matches `Bash` and runs `$CLAUDE_PROJECT_DIR/.claude/hooks/no-shell-edits.sh`. All three documents are held to naming only files that exist by `tests/repository-docs.test.ts`.

[Backlog](docs/BACKLOG.md) holds the work that is understood but not done, with enough detail that picking it up costs nothing.

Rules that can be automated are automated, following the principle that a ratchet beats a checklist. Several run as tests, each written after the repository had already broken the rule it now enforces:

- `tests/repository-docs.test.ts` re-derives every version and workflow badge in this README from `package.json` and `.github/workflows/`, and checks that every file and symbol this README names still exists. A README once described a helper that had been deleted. What it searches has strings taken out as well as comments, which is the second half of that same lesson: a name surviving only inside an English sentence in a test fixture is not the repository having it, and the check was being satisfied by exactly that — `PreToolUse` is named in three documents and appeared in the code in one place, the prose explaining that the entry does not exist. The check for a document naming something gone was answered by the document's own account of why it is gone. Two things deliberately keep their strings: `.claude/settings.json`, because it is JSON and every key in it is one, so stripping it removes the only trace of `SessionStart`; and a template literal's `${…}` holes, which are code. The repository's own filenames are in the haystack too, since a module is something the repository has whether or not any identifier spells it — `portfolioData` is a real file that nothing names outside an import path. What is left after all that is a short list of names the documents use legitimately and no identifier defines — each entry with its reason, and its length is the list's to say rather than this sentence's, which counted four while it held five — and the list fails on an entry that has stopped appearing anywhere or that the code turns out to define after all.
- `scripts/runPrintCheck.ts` prints the site to PDF, reads the sheets back and reports when the layout stops matching the one recorded in `scripts/printedCv.ts`. Sections and job entries carry `break-inside-avoid` so that an entry is never split across two sheets; the cost is that a block too tall for the space left moves to the next page whole, which is why the CV runs to six sheets with 38% of the first and 58% of the fourth blank. That is a deliberate trade, so the check records the shape rather than judging it. Nothing on a screen shows any of this. It also prints a second time with the privacy dialog open and fails if that document differs from the first: the dialog shell portals into `document.body`, which puts it outside the `print:hidden` wrapper the screen page sits in, and a fixed element repeats on every printed page — so printing while reading the policy put its text on all six sheets and left 96% of each one dark. Stated as a relationship rather than a list of forbidden words, because a word list would have to be kept in step with the dialog's prose and the first edit to it would quietly empty the check. "The same document" turned out to be four claims and the first version of it made one — a set of *added* strings, one direction, no emptiness guard on the second print. So a print that came back textless read as identical, text a dialog removed was invisible at equal sheet count, a repeated string was invisible at any, and the recorded defect's other half went unmeasured: moving `print:hidden` from the dialog's shell to its panel takes the words off paper and leaves the backdrop on it, 61% of every sheet, without changing one string. It rasterises both prints now and compares the ink as well as the text, and `PLAUSIBLE_INK` reads that number back — a comparison between two prints cannot report an instrument that has stopped reading the page, because the same wrong number on both sides is agreement. The deciding lives in `scripts/printedCv.ts` and is held in the fast suite by `tests/printedCv.test.ts` and `tests/dialogOnPaper.test.ts`; only the printing needs a browser.
- `tests/wiring.test.ts` fails on any module that no test reaches *through the code that uses it*. A unit can be correct, tested and connected to nothing: this repository lost the error boundary from its root, the contact dialog's reset on close, and the hook that honours a shared link to a section — three times, each caught only by hand, each with the whole suite green. Modules the graph legitimately cannot reach are listed with a reason, and the list may shrink and may not grow.
- `scripts/withoutComments.ts` is the one comment stripper the graph and both documentation gates share. There were three copies of the rule and the import graph was the one without it — the graph both reachability ratchets read. This repository's comments name modules and describe their removal, which is prose that reads as code to a regular expression: the dependency gate was once reported an undeclared package called `in front of\n * it`, from its own doc comment, and the import graph was extracting a specifier out of that same comment until this landed. Proven on a two-file fixture: a caller whose only mention of a sibling was a comment saying the import had been removed reached it.
- `tests/module-reachability.test.ts` walks the import graph from the entry point and the stories, and fails on any module nothing imports. The repository previously carried ten duplicated section components that had drifted from the live ones while every check stayed green.
- `tests/sectionWrappers.test.ts` fails on a page section that writes out its own wrapper instead of using `src/components/ui/PageSection.tsx`. Ten sections each carried the same anchor, reveal and heading — twenty-three lines with four words changed — and one of the ten had already drifted to a longer slide and a slower easing with nothing to notice. Copying a neighbour is how that happened, so the exemption list is named with reasons and may shrink but not grow. It started at four entries and is now one: the component the others use.
- `tests/dependencies.test.ts` fails when an installed package refuses the installed Storybook core, when the source imports a package it does not declare, or when the CI install step reaches for `--legacy-peer-deps`. That flag had been hiding a conflict which made `npm install` impossible for a whole major version.
- `tests/animationFeatures.test.ts` fails if the motion feature set widens back to `domMax`, which renders identically and costs the saving, so nothing at runtime would report it.
- `scripts/runLighthouse.ts` audits the built site and fails if any category scores below what the badge at the top of this README claims. It serves the built output compressed, as the host does; `tests/staticServer.test.ts` holds it to that. The first version did not, which put 380 kB of JavaScript on the wire instead of 118 kB and made the page measure 86 on mobile emulation where it actually scores 98 — a defect that looked like it was in the site and was in the measurement. The badges are not duplicated as thresholds; they are read out of this file and used as them, so lowering one to pass a build is a visible edit to the claim. They had stood at 100 since before any of this work, backed by nothing — and the first run showed why that matters: unlabelled, they were the desktop numbers, and they are the desktop numbers now, measured and labelled as such. Reading the claim out of this file is the part that decides whether the audit means anything, so it is held in the fast suite by `tests/lighthouseBadges.test.ts` — read the wrong number out of the README and the gate measures the site against nothing.
- `scripts/runRevealMotion.ts` drives the built page in a browser and counts the distinct positions a section passes through as it arrives, with and without `prefers-reduced-motion`. Twelve entrance animations across nine files ignored the setting while the particle canvas and the 404 view honoured it, so the page disagreed with itself: a section slid the same way whichever the visitor had asked for, and lands in one move now when the preference is set. The gate holds it to at most `ARRIVING_AT_ONCE` positions with the preference and at least `SLIDING` without, and does not hold it to a figure in between: four runs of one build measured the same section at 24, 26, 23 and 26, so a number quoted there would be a number about that afternoon's browser. The reduced-motion side has no such spread — two, on every section of every run, because the transform is gone rather than shortened. The fade is deliberately left alone — a fade is not what makes motion unbearable. This needs a real browser, because motion reads the preference through `matchMedia` at load and under jsdom both settings animate identically, so a test there passes whether or not the page honours anything. What counts the positions and decides on them is `scripts/revealMotion.ts`, held in the fast suite by `tests/revealMotion.test.ts`.
- `scripts/runFocusIndicator.ts` tabs the built page and checks, in pixels, that every keyboard stop shows where the keyboard is. Twenty controls spelled out their own focus ring in two variants and eight — every link on a project card — declared nothing at all and fell back to the browser's own, so twenty-nine stops carried four different indicators. There is now one, `focus-ring` in `src/index.css`. It has to be a browser and it has to be pixels: a computed style reports a colour Chrome does not paint, and a control mid-transition reports a ring it is still fading in. It also walks the page at two widths with the consent banner showing, because a banner pinned to the foot of the viewport hides whatever the browser scrolls under it and the browser has no idea it is there: two controls were entirely behind it when they took focus (WCAG 2.2 SC 2.4.11), five were unclickable — including two the banner did not visually cover at all, since its band runs the full width of the viewport while its card does not — and the scroll-to-top button took a third of the Accept button at 768px, so a tap on the right of Accept scrolled the page instead of recording a choice. Both walks assert how many stops they found against a recorded number, because a floor is the wrong instrument for a sweep that can stop early: the keyboard walk was floored at "more than 20" while the page has 28, so a truncation to 21 — seven controls never checked — reported "21 keyboard stops; 21 show where the keyboard is" and exited 0, and the sweep under the banner had no floor at all, its count accumulated and printed and never asserted. Recorded per width, since the page reflows and the order is 29 stops long at 1280px and 23 at 768px — 28 and 22 until the TestingLab card's second edition put one more link on the page, and this sentence said the old pair for a while after the script recorded the new one; the first attempt derived one number for both by arithmetic instead of measuring, and it was wrong at both. The first version of this gate checked only that *something* was painted and passed the very defect it was written for — Chrome draws a ring where the page draws none — so it checks the colour too. It also samples the banner's own buttons on six short viewports with the navigation's mobile menu open, because two widths at one comfortable height proved nothing about a short one: that menu grows downward until 16px from the foot of the screen, so at 812×375, 740×360 and 768×500 it covered Accept and Decline whole, and a tap on Accept reached a nav button — the page scrolled away and no choice was recorded. That is the second pair of things pinned to the same corner to find each other here, so the menu subtracts `--fixed-bar-space` from its height, the same property the body's padding spends. Subtracting it with no floor was the next defect: below 640px the banner stacks and takes most of a short screen, and the menu computed to its own padding and no rows — zero of seven items at 320×320, 480×320 and 568×320, where the rule it replaced showed four. Those three widths are in the sweep now, and it asserts the menu keeps rows as well as asking whether the banner's buttons are covered, which is the half it was missing. The floor is 120px because the banner's policy link sits 132px below the menu's top edge; the first attempt was measured against Accept at 171px and the gate refused it. One control gets a pass of its own: the scroll-to-top button exists only past 300px of scrolling, so a walk that starts at the top cannot reach it, and an earlier fix for an unstable sweep reloaded to a state where it is absent — which made the sweep steady by leaving a control out of it. Measured: with `focus-ring` stripped from that button, leaving no indicator at all, the gate printed "28 keyboard stops; 28 show where the keyboard is" and exited 0 while this paragraph claimed it checks every stop. It is now scrolled to, stamped and measured like the rest, and the gate refuses if it cannot be found — 29 stops. What decides a verdict from the pixels lives in `scripts/focusIndicator.ts` and is held in the fast suite by `tests/focusIndicator.test.ts`, and the source-level half of the same rule is `tests/focusRing.test.ts` below.
- `tests/cvSections.test.tsx` fails on a headed section of the printed CV written by hand instead of through `src/components/CvSection.tsx`. Seven of them carried the same two print rules and the same nine-class heading, all seven identical — which is the fragile case, because the next hand to touch one makes it six and one, on the page nobody sees on a screen. `print:break-inside-avoid` is what the printed layout is made of, so the check above would catch it losing that rule, but only after the layout had already changed. It counts the sections in the rendered document now rather than in the template's source. It used to count `<CvSection>` calls and expect seven, which was the right question while seven were written out and the wrong one the moment they became a loop over `src/content/cvLayout.json` — counting the source would have reported one section and passed on a template that drew nothing. What the check always meant was *every headed section comes from the shell*, and rendering is the only way to say that once the count is data.
- `ContactModal.stories.tsx` holds the contact form in the state a visitor spends the wait in, because every other story scanned it at rest and that was the one state nothing looked at. The submit button rendered a bare spinner in place of its children, so from the moment Send was pressed until the request answered it had no accessible name at all — axe reports `button-name`, and a screen reader announces "button, dimmed". `Button` already carried an `isLoading` prop written for exactly this, which renders the spinner beside the children and now also sets `aria-busy`; no production caller passed it. An optional parameter nobody passes compiles everywhere and stays silent exactly where it was forgotten.
- `tests/contentReaches.test.tsx` says that every word in `src/content` reaches a reader: each of the 392 reader-facing strings across the 14 content files, with `{{yearsOfExperience}}` filled in first, has to appear in the page's text or the printed CV's. It replaces a characterization snapshot that recorded all 33 kB of that text, and the replacement was forced by the thing the snapshot was written to enable. Measured: changing one word of one quote in `src/content` turned the suite red, because the snapshot records the page and an editor's whole purpose is to change the page. An editor that cannot commit without also re-rendering the page in a test runner is not an editor, and `vitest -u` from a web app is not a plan. So the invariant is restated as a property, which holds under any edit an owner makes and fails on what the snapshot was really there to catch — content that stops arriving. It is also the guarantee a content editor needs most, stated where it can be enforced: *if you write it, it shows up*; a component rendering three of four paragraphs is silent on the page and loud here, which is the mutation it was proved with. Together with `contentBoundary` it closes both directions of the bargain — that one says the page invents nothing content does not offer, this one says the page drops nothing content does offer. The one exception list is by key and not by value — `icon`, `accent`, `tone`, `url`, `href`, `imageUrl` and the two image dimensions are not text anybody reads — because a per-string exemption list is a list that grows. What went with the snapshot and is not pretended otherwise: the wording and the order of the prose still written into components, which nothing holds now. Covering it was tried and measured as unworkable in the obvious form: striking content strings out of the page text to leave a remainder shreds it into 255 fragments, because short entries like the two-letter and three-letter tags match inside longer sentences. The copyright year went with it too and was the only thing the snapshot uniquely held, so it is kept in `src/components/layout/Footer.test.tsx`, asserted the way the hero's years figure is — by moving the clock, since asserting today's number passes against a literal just as happily.
- `tests/storyCoverage.test.ts` fails on a component in `src/components/ui/` with no `.stories.tsx`. Both guideline documents already required one and neither said it to anything that runs, so four components were added without one and the documents went on claiming otherwise for two pull requests. The rule is not paperwork: `.storybook/preview.ts` sets the accessibility addon to `test: 'error'`, so a story is an axe run that fails the build, and a component with no story is one that scan never sees on its own. It also fails on a story file that exports no story, because asking whether a file exists is a proxy for the property rather than the property — a `.stories.tsx` with a `meta` default and nothing else satisfies the filename, indexes zero stories, and gives the addon nothing to scan.
- `tests/documentedStructure.test.ts` re-derives the project tree drawn in `AI_INSTRUCTIONS.md` from `src/`, in both directions — a directory missing from the tree and a directory the tree invents both fail. It compares paths rather than names, because a bare name is not unique across levels and keying on one made the check answer a different question: with the ten drawn names held as one flat set, a new top-level ui or sections directory was reported as drawn because the name already appeared a level down, and the tree could invent a data directory under components and pass on the top-level `data/`. `src/lib/` and `src/test/` had existed for some time without appearing in it, so a document telling a contributor where things go was silent about two of the places they go.
- `tests/dialogShell.test.ts` fails on a dialog that builds its own shell instead of using `src/components/ui/Modal.tsx`. The keyboard and focus behaviour was already shared, and that is the half where a mistake is obvious; the markup around it is the half where a mistake is silent. A dialog that forgets `aria-modal`, or labels itself with an id that resolves to nothing, still opens and still looks right.
- `tests/sourceMaps.test.ts` builds the site and reads the output, failing if a script ships without a map, without a `sourceMappingURL`, or with the map inlined into what every visitor downloads. Error reports carry a position inside the minified bundle, which is exact with a map beside it and worthless without one, and nothing at runtime would report the difference.
- `src/components/ErrorBoundary.test.tsx` reads `src/main.tsx` and fails if the error boundary is not mounted around the app. The component keeps passing its own tests and its own story while wired to nothing, so removing it from the root was previously invisible.
- `tests/repository-docs.test.ts` also fails on a comment citing a governing document by a name nothing goes by any more. Renaming the governing document left four such citations behind — in `scripts/lighthouse.ts`, `tests/dependencies.test.ts`, `tests/module-reachability.test.ts` and the docs test itself — and nothing turned red, because the path check only sees a backticked path and the symbol check only sees an identifier. A document named in prose is neither, so the rename read as complete while four comments pointed at a file that no longer existed.
- `tests/hookRegistration.test.ts` holds three things together: the hooks on disk, the commands `.claude/settings.json` actually invokes, and what these documents claim about them. `no-shell-edits.sh` was written, hardened twice and merged — and for a long while never registered, so it never ran, while this README said it ran before every shell command and CLAUDE.md said it enforced both rules. Its own tests could not see that: they run the script directly, which is the right way to test a hook and says nothing about whether anything invokes it. An unregistered hook may stay, named with its reason in a list that may shrink and not grow, but the documents then have to say it does not run — and the moment someone registers it, the list entry and the caveat both fail until they are removed. That is what happened, and then it happened in reverse. It was registered, it ran for about two hours, and it refused ordinary work four times: a branch made in a compound command, a commit message with an apostrophe in it, a scratchpad path held in a shell variable, and a `grep` whose pattern contained a `>` inside quotes. Three were fixed. At the fourth the owner switched it off, which is the right call — each was the same defect, a script parsing shell text with regular expressions where the shell parses it with a shell, and two adversarial audits had read that file and found none of them. It is back in the list with that as its reason, the caveats are back, and the rule it enforces is held by attention. The file and its tests stay: what it cost and what it caught is worth keeping, and `docs/BACKLOG.md` draws the general lesson from it.
- `tests/shellEditGuard.test.ts` runs `.claude/hooks/no-shell-edits.sh` as a process and checks it refuses a shell file edit and allows ordinary shell use. It tests the hook rather than a function inside it, because what goes wrong with a hook is that it never fires — a bad shebang, a missing interpreter, a payload that does not parse — and a unit test of the patterns passes through all three. That comment named those three failures and asserted none of them, and the middle one was live: the hook read its payload with `jq`, `set -uo pipefail` carries no `-e`, so on a machine without `jq` the tool name came back empty and every shell edit was allowed while the suite reported them refused. It fails closed now — a missing interpreter, unreadable JSON and a command that cannot be split all refuse — and the refusal path uses no external command, because the first version of it reached for `cat` to say that nothing was on the PATH. Two audits measured fifteen ways past it between them, nearly all the same mistake: a shell command taken apart with regular expressions and word splitting. `shlex` splits it the way a shell does, which is what a quoted path containing a space needs, and `json` reads the payload, which removes the dependency whose absence used to open the gate. The cases were a quoted redirect target, `>|`, a quoted path to `git checkout`, a relative path after a `cd`, a redirect with no space before it, `sed` with `-i` after another flag, a glob no single file answers to, and `git` with a flag before its subcommand. The first was `cat > "src/App.tsx"`, which is the first row of the hook's own test table with a pair of quotes on it — the extraction excluded quote characters from what a target may look like, so a quoted target produced no match at all and the branch had nothing to judge. The last is answered by refusing rather than by finding nothing to object to: after a `cd` the hook cannot resolve a relative path, and a guard that cannot see should not report that there is nothing to see. This is the one ratchet aimed at how the work is done rather than at the code, and it exists because writing the rule down in two documents did not stop it being broken: the harness re-states the opposite preference every turn, and repetition beats recall.
- `tests/consentBeforeTag.test.ts` reads `index.html` as a visitor receives it and fails if it reaches for the analytics host on its own. Consent Mode kept cookies unwritten, so this looked handled and was not: `<script async src="…googletagmanager.com…">` sat in the head and the request went out on every visit, before the banner had been answered, carrying the visitor's address, user agent and referring page — with a `<link rel="preconnect">` above it opening the connection sooner still. The privacy policy this site serves states that no measurement data is collected until the banner is accepted, and this is what holds the document to that sentence. It also asserts what must still be there, because the fix for "loads no analytics script" is one deletion away from a page that never measures anything and never says so.
- `tests/consent.test.ts` holds the three places the visitor's analytics choice is spelled to one constant. They cannot import each other: the app reads it through `useCookieConsent`, `index.html` reads it in plain JavaScript because the choice has to be re-applied before Google's tag script loads — after that the tag has already decided whether to write — and the privacy policy quotes the key to the visitor as a statement of fact about where their data is kept. Nothing tied the spellings together, so a rename would have broken the second one in silence: the visitor who once opted in simply stops being counted, and the site looks entirely healthy.
- `tests/botLayer.test.ts` holds `public/llm.txt` to `src/data/portfolioFacts.ts`. It restates the same jobs, awards and certifications for language models rather than people, and unlike the JSON-LD beside it nothing generates it. The asymmetry is the argument: a person notices when the page is wrong, and nobody reads their own machine-readable profile, so a stale employer or a dropped credential is what every crawling model is told for as long as it takes somebody to think of checking.
- `tests/structuredData.test.ts` covers the Person schema that used to be typed out in `index.html` and is generated now. The first assertion is the characterization taken before the hand-written block was removed — a refactor is not the place to change what a site claims about itself — and the rest hold the output to the data rather than to a copy of it, check that it is what actually reaches the built page, and refuse a page with nowhere to put it.
- `tests/declaredImageSizes.test.ts` re-derives the dimensions this repository states about its own images from the PNG headers. The banner at the top of this README declared 1200x475 for an image that is 1200x630, so GitHub squashed it by a quarter on the repository's front page — the first thing anyone sees — and nothing anywhere could have reported it. The preview image's ratio is checked as a band rather than a figure: 1200x630 is 1.905, not the 1.91 that gets quoted, and asserting the quoted number to two decimals failed the correct image.
- `tests/portrait.test.ts` reads the intrinsic size out of the WebP container and fails if the page declares something else, if the file is larger than the page can use, or if it outweighs the bundle. The photo was 2008x3119 and 482 kB for a box at most 600px tall, fetched from an absolute URL on the live site, and declared as 600x600 against a portrait, which moved the page by 0.0223 CLS when it arrived. The largest single download on the site was a photograph, and a file four times larger than it needs to be looks exactly like a correct one from the code.
- `tests/focusRing.test.ts` is the source half of the focus rule: `focus-ring` is declared once in `src/index.css` and used by name everywhere else, with the one control that draws its own named beside the reason. The browser sweep above proves what a stop paints; this is what stops the twenty hand-written rings from coming back one component at a time. It also asserts the class is an outline rather than a ring with an offset, because an offset paints the background colour into the gap and so has to know what is behind it — which is what made two variants necessary and `#020617` wrong on a modal.
- `tests/contactParts.test.tsx` holds what `src/components/ui/ContactParts.tsx` protects, and it exists because that protection had never been written down. The contact details are stored as parts — `["hello", "@", …]` — so that rendering them leaves no text node carrying the whole address, which a harvester walking text nodes would take. Nothing said so and nothing checked it, and it cost twice. `ErrorBoundary` rendered `display.join("")`, putting the address back together on the one screen a visitor sees when something has broken. And the phone number was being announced to a screen reader as one run of twelve digits, on the page and on the printed CV, because an accessible name is built by trimming each element's contribution and concatenating with no separator — measured: fragmented it reads `+48530333243`, whole it reads `+48 530 333 243`. So the protection had a price and nobody had priced it. One component decides now, and reads the trade off the data rather than a flag: fragment a detail when no part of it is a space, render it whole when one is. That keeps the protection where it is free — the address, which is what spam harvesting is about — and drops it where a person listening pays. An `aria-label` would buy both and is refused, since it puts the whole value in an attribute, which is the first place anything scraping a page looks. The check holds four things: no text node carries the address, both details still announce exactly, the decision comes from the data, and no component other than that one reads `.display` — which found a third consumer nobody had mentioned, `PrivacyPolicyModal`, the moment it was written.
- `tests/declaredDomain.test.ts` holds the eight places this site types its own address to one: `public/CNAME`, the page's own data, the printed QR's `PRINT_URL`, `index.html`'s canonical and its four social tags, `sitemap.xml`, `robots.txt` and `llm.txt`. They were eight independent copies of one string, and a domain that moves takes some of them and leaves the rest pointing at an address nobody owns — which nothing on a screen shows. `CNAME` is the copy that stopped being ordinary: this repository used to be named `<owner>.github.io`, which GitHub Pages serves as a user site from the root, and it is named for the domain now, which Pages serves as a project site from `/<repository>/` unless a custom domain says otherwise. `CNAME` is what says otherwise, `vite.config.ts` sets no `base`, and every asset URL the build writes is absolute from `/`. So losing that one file no longer means losing a nicety — it means every asset 404s under a path the build has never heard of. Nothing in the code changed for it to become load-bearing, which is why a check is worth more here than remembering. The page's own data is the source of truth rather than the config that deploys it, because that is the copy a visitor reads and the one the rendered-content snapshot already holds.
- `tests/exportUse.test.ts` reads the TypeScript compiler's own view of the program and fails on a value carrying `export` that no other file ever names. Both reachability ratchets work at the level of a module — they ask whether anything imports the file — so an export inside a file that *is* imported, and that nothing refers to, is invisible to them. The compiler rather than a text scan, and it earned that on the first measurement: `grep -rl PLAUSIBLE_INK` reported `scripts/runPrintCheck.ts` as a consumer of a constant it mentions only in a comment about what holds a number honest. Seven keywords came off. The narrowing is deliberate and measured: of 231 exports, 85 have no consumer outside their own file, and almost all are legitimate — 37 are types, which cost nothing at runtime and state a component's contract; 32 are units a module split out so a test could reach them and then calls two lines down; 9 are gate logic or test infrastructure whose consumers are gates and tests by construction. Types and default exports are left alone, the second because a default is imported under whatever name the other side picks, so counting the files that write `default` answers a different question. The honest limit is recorded with the check: it would *not* have caught the walk-termination helper that prompted it, since that had four tests and so had references. Telling "tested and used by the gate" from "tested and used by nothing" needs to know whether a runner calls it, and the same module's other exports are read by the same tests and by no runner while being entirely legitimate — there is no structural line there, so that class stays held by reading and the backlog says so.
- `scripts/runTargetSize.ts` measures the built page's pointer targets and fails any below 44×44, which is WCAG 2.2 SC 2.5.5 Target Size (Enhanced); `tests/targetSize.test.ts` holds the deciding half in the fast suite. The enhanced level rather than the AA minimum of 24×24, and not out of ambition: this page already holds itself to AAA on focus with a browser gate that measures it in pixels, and a page that is AAA for the keyboard and AA for the thumb has picked its level twice and by accident. Nothing measured this at all. Of 79 targets across 1280px and 375px, **58 were under the bar**: the eight icon links on the project and initiative cards were 28×28, the control that opens the whole navigation on a phone was 32×32, the seven desktop navigation links were 20px tall with no vertical padding at all, and the two buttons a visitor has to hit to answer the consent banner were 32 — on the one surface where a miss costs most, since that banner sits in the corner an iPhone reserves for its home indicator. Every fix was padding rather than size, so the type and the icons are where they were and nothing on screen changed size; the navigation's row gap paid for the width its buttons gained. What the sweep has to do or it reports nothing useful is most of the work in it. It focuses each control before measuring, because the skip link is `sr-only` until reached and measures 1×1 otherwise — the page's most deliberate piece of accessibility work would have been reported as its worst failure. And it visits every state whose controls do not exist in the page as it loads: the mobile menu, whose items are absent while it is shut, and both dialogs, which portal in only while open, and the scroll-to-top button, which is mounted only once the consent banner has been *answered* and the page is 300px down — so no amount of scrolling on a fresh load reaches it. That last group is the defect this gate shipped with and was caught by its own author before it merged: measuring only the loaded page found 79 targets and passed all 79, while both dialogs' close buttons sat at 32×32 unmeasured, which is the way out of a dialog for anyone using a pointer. It takes 250 measurements now — measurements and not targets, since the controls present throughout are measured again in each state, and deciding what counts as the same control across two widths and five states is a judgement the count does not need to make. The count is recorded and held exact, not floored — the same lesson the focus gate learned twice, and its own: a floor let the recorded number fall behind the page's real 250 while a truncated sweep would still have passed, so the second bughunt made this match in both directions, the way the focus gate already did.
- `tests/printQrCard.test.ts` draws the card the build serves at `/cv-qr-code.png`, decodes the PNG, samples the centre of every one of its 841 modules and rebuilds the matrix, failing if it is not the code for `PRINT_URL`. That address is on material this site does not control and cannot recall, and it answered 404 from the day the two files behind it — a PNG and an SVG, for a QR the print template had stopped rendering — were removed as assets nothing pointed at. It comes back drawn at build time rather than committed, because a generated artifact checked into a repository is free to drift from what generated it, and it is written straight into the build output and never into `public/`, which is why it needs no exemption from the ratchet that removed its predecessors. That leaves it the one shipped artifact here with no other guard: no import reaches it and the unused-asset check cannot see it, so this reads the image back instead of checking that a file was written. Three things it holds that nothing else could: that the code still encodes the address, that the white margin a scanner needs is still around it — the CV's own drawing has none, and on paper the sheet supplies one by accident — that its one line of text has not run off the card, which text does not refuse to do — an earlier version of this card carried a name and ran it off the right edge at a fixed 58px, silently; and that the code still sits in the site's gradient frame, running cyan into purple, which is the only thing on the card that says whose the code is and which nothing asserted when it was added — taking the frame to nothing left every other check green. The frame is sampled inside its own band, because the glow spreads the same colours over the ground for another forty pixels and a probe out there would pass on the glow alone. The card shows the code and one caption and nothing else: it is read at 345px across in a LinkedIn featured tile, where the name would be the fifth copy of itself in one view and would take its height from the only element a camera has to resolve. The margin check follows the caption's own band, since the version written for the layout with a name watched a strip where a name used to sit and caught an overflow only because a large enough glyph happened to reach into it. It also reads the plugin list out of `vite.config.ts`, since everything else here judges a card the test drew itself and would stay green with the plugin unhooked and the address answering 404 again. Two of its checks were tautologies when written and were caught by mutation: the size was compared against the constant that produced it, and the code was drawn from an address the test supplied rather than the one the build ships.
- `tests/icons.test.ts` exists because an icon stopped being a symbol and became a string. While a card was written as `<Terminal size={24} />` a wrong icon was a compile error and an unused import was a lint error; once the cards' 79 sentences moved to `src/content` the icon moved with them as `"icon": "Terminal"`, and both of those went away at once. So `src/data/icons.ts` is a registry of the icons content may name, and this holds it in both directions, because the two failures differ and neither is visible. A name the registry does not have throws at load — chosen over a placeholder icon, since a page that renders wrong is a page nobody re-reads — and a registered icon no card asks for is reported here, since nothing else can see it: the import *is* used, by the registry, which is precisely what hides it from lint and from both reachability ratchets, at roughly 400 bytes of bundle for nobody. The narrowing is the point rather than a side effect: lucide-react ships about fifteen hundred, and the list an editor picks from should be one somebody decided on. Once an editor existed, "decided on" stopped meaning "used by a card": an owner adding a card wants a choice, so `OFFERED_ICONS` in `src/data/vocabulary.ts` is the one licence — twenty-four icons chosen for this site's subjects, listed as on offer and nothing else, at 6.1 kB of the built page, 2.5 kB gzipped, measured — and the check reports any registered icon that is neither worn nor on that list, and any on that list a card has since taken, so the offer stays what it says it is. It caught two things on its first run. The project card imported `Code2`, a deprecated alias whose canonical name is `CodeXml`, so the two names for one icon disagreed the moment the icon became a string — and every text check stayed green, the 33 kB content snapshot included, because an icon is not text; it is registered under the canonical name now, an alias being what a major version removes. And the accent key was called `tone`, which is already the award cards' key for a *different* palette — gold, cyan and purple against cyan, purple, emerald and orange — so one key carried two vocabularies, which reads as a single palette to anything building a form from the content and would have offered the wrong four colours in three places. Renamed to `accent`. What the check cannot see is whether a card draws the icon it drew before: that was measured once, by fingerprinting all 37 icons with their sizes and classes against the pre-migration module, entry for entry, and mutating one accent to prove the comparison could fail.
- `tests/contentShape.test.ts` catches the failures a form makes, and carries a decision about what it deliberately is not. There is no JSON Schema in this repository and no validator dependency, because what a schema would buy was measured against what already holds, one broken content file at a time: a required field removed and a wrong type both fail `tsc`, which types a JSON import from the literal it finds; an unknown extra key fails `tests/contentReaches.test.tsx`, since a value nothing renders is a value nobody reads; and a bad icon, accent, tone or placeholder throws at load. That is most of what a schema is for, already enforced, and a second description of the same fourteen shapes would be a second thing to keep in step with the first — this repository's oldest complaint. What none of it catches is content that is the right shape and empty, which is exactly what an editing form produces: a field cleared, a list emptied, a value pasted with the space that came with it. Measured before the check existed: `hero.name` set to the empty string left the page's `h1` blank and `about.paragraphs` set to the empty list emptied a whole section, and both passed `tsc` and all 591 tests. So four rules over the whole tree and no per-file description — no empty or whitespace-only string, no untrimmed string, no empty list, no number that is not positive — universal on purpose, so a content file added next month is covered without anyone remembering to describe it. The single exception is the contact parts, whose phone number carries three single spaces as its grouping, and it is exempt by key with two guards of its own: one fails if the phone stops being stored in parts, leaving an exception that protects nothing, and one fails if a second file gains a `display` key, which would widen the exception without anyone saying so.
- `tests/cvLayout.test.tsx` holds the printed CV's shape, which stopped being written out and became `src/content/cvLayout.json`. Which sections appear, in what order, under what heading and with which icon are what the original request called *defining the CV layout*, and all four were unreachable except by editing a `.tsx` file: seven `<CvSection>` calls with their headings typed beside them, ordered by where they sat in the JSX. What did **not** move is what each section draws — seven genuinely different shapes, a justified paragraph, a grid, a timeline, three flavours of list and a nested list — because a shape is code. Content names one by key and `src/components/CvBodies.tsx` resolves it, throwing on a key it does not have; the check holds that naming in both directions, since a shape left behind after its section was dropped is unreachable code that lint calls used, the record holding it being used. It also reads the rendered headings back and compares them to the layout's order, so it is the document that is checked and not the intention. The move was prompted by a defect measured on the pre-change code in a worktree, and it is the class the content split makes *more* likely rather than less: the three certification groups' icons were chosen by comparing each group's heading against three string literals in the template, so renaming `Core certifications` to `Core Certifications` — one capital, an edit an owner is now invited to make — took the printed CV from three group icons to two, with `tsc` green and all 595 tests green. The icon is named by the group in content now, which makes the same rename render three and three.
- `tests/pageLayout.test.tsx` holds the page's own shape the way `cvLayout` holds the printed CV's, and carries the better half of that pair. Thirteen bands were thirteen calls in `App.tsx`, and the ten with a heading typed it inside their own component along with `number="01"` through `number="10"` — ten hand-written strings in ten separate files with nothing holding them to each other. Measured before the change: two bands could both carry `03`, or one could carry `14` out of a run of ten, and `tsc` passed and all 602 tests passed; inserting a band meant renumbering by hand every band below it. Guarding that was the obvious answer and the weaker one. The number is read off the position in `src/lib/pageLayout.ts` instead, so it cannot disagree with the position — the defect stops being caught and becomes unsayable, which is worth more than a check. Measured after: moving Skills to third place renumbers Skills to `02` and everything under it, with no file edited. `PageBand` is a union rather than a record with two optional keys, so a heading and an anchor cannot come apart — an optional pair would allow a heading whose anchor the navigation scrolls to nowhere. The rest is the same bargain as the CV's: which bands appear and in what order is content, what any one of them draws is `src/components/PageBodies.tsx`, and the naming is held both ways, the unused direction being invisible to lint because the registry uses every import. It also checks what nothing did before — that every anchor `NAV_ITEMS` scrolls to is one a band actually carries, the layout and the navigation being two lists that agreed only by hand — and it reads the printed numbers back out of the rendered document, since everything else in the file is arithmetic on the content and none of it says the page renders the result.
- `tests/vocabulary.test.ts` holds what the content editor is told it may choose to what this site will actually accept. The editor is a separate program in a separate repository and never compiles this one, so the names it offers — forty-eight icons, eight accents, six award tones, seven CV section shapes, thirteen page bands — reach it over HTTP from `/vocabulary.json`, written at build from `src/data/vocabulary.ts` by a plugin in `vite.config.ts`. The check that the served file matches the module needs a build to read, and `npm test` runs before `npm run build`, so in CI it had never run at all: `check:quality` now runs it again after the build, as `test:built`, with the bundle check in `tests/contactParts.test.tsx` that had the same gap. The plugin writes into the build's own `outDir`, resolved from the config, and only for the site's build — Storybook builds through the same config and the source-map test builds into a temporary directory, and both used to get the repository's own dist directory freshly stamped with a sitemap date, a QR card and this file under a page from an earlier build, which is what those two post-build checks then compared against. Emitted rather than committed, for the reason the print QR card is: a generated file checked in is free to drift from what generated it. Its alternatives were both worse — a second copy of every list in the editor's repository, which is the failure `declaredDomain` exists for, or no list at all and a free-text field letting an owner type a name the site throws on. Most of the agreement is the compiler's rather than this file's: each registry is written `satisfies Record<Name, …>`, which is checked in both directions, measured — a missing key is TS1360 and an extra one TS2353 — and `AwardTone` is derived from the list rather than declared beside it, so the type and the list are one thing. What is left for a test is what the compiler cannot see: that the plugin is still registered, that the served file matches the module, and that a name on offer is a name some content file uses, which is the direction a list quietly grows in — with icons the one exception, where what is spare has to be exactly `OFFERED_ICONS`, held both ways. The registration check was a tautology when first written and mutation caught it — `expect(config).toContain("vocabularyPlugin()")` matches the function's own declaration, so unregistering the plugin left it green; it resolves the config and reads the plugin names now, which is what `tests/printQrCard.test.ts` had already learned. The same licence the icons have covers the accents and the award tones: `OFFERED_ACCENTS` and `OFFERED_AWARD_TONES` are the names on offer to the editor that no card wears yet, and the check holds each list's spare names to exactly its offer, both ways.

- `tests/accentContrast.test.ts` measures every accent and every award tone as text on the card it colours, at 4.5:1 or better, the way WCAG 2 computes it. The axe run in Storybook measures contrast too, but only on what a story draws, and a story draws what content uses; an accent on offer to the editor that no card wears yet is drawn by nothing, so the one check that would refuse it never saw it, and the owner picking it in the editor would have been the first to find out. This reads the colour behind each `text-<hue>-<shade>` class from Tailwind's own theme and holds it against the ground the cards sit on, `#0a1128` at 80% over the page's `#020617`. Measured when it was written: every 400 shade Tailwind ships clears the bar by a wide margin, the lowest at 6.1:1, because the ground is nearly black; a 500 shade of violet or indigo is under it, and a shade is one keystroke from another, which is why the bar is held rather than noted. The four accents and three tones added with it — rose, amber, lime and blue; emerald, rose and blue — were chosen by hue, each at least 28° from every colour the cards already wear, so each reads as its own.
- `tests/sectionIdentity.test.ts` makes thirteen orientation comments load-bearing. A section component used to tell a reader its heading, its number, its anchor and its shape in one file; those four moved to `src/content/pageLayout.json` so somebody outside this repository could arrange them, and what was left was a bare grid saying nothing about where it appears. `AI_INSTRUCTIONS.md` carries the reading map for the whole path — this is the pointer at the other end of it, in the file somebody actually opened. Checked rather than written and left, and the reason was one file away: `portfolioData.tsx` carried a comment saying everything in `portfolioFacts.ts` was re-exported through it so a component could import the whole set from one module, and nothing was — there was no re-export and there never had been, and everyone had read past the sentence for as long as it existed. So each comment names its band in backticks and this holds that name to the registry and the layout, in four ways, each proved by mutation: a component that says nothing about which band it is; one naming a band it is not, which is how thirteen near-identical comments actually go wrong — pasted from a neighbour with the name left behind, reading perfectly; one claiming to render its own element when the layout gives it a heading, a difference invisible from inside the file; and one orienting a reader without saying where to look next. What a comment deliberately may **not** repeat is the heading itself: that is content, an owner may rewrite it from the editor, and a comment quoting it would be stale the first time they did.
- `tests/sitemap.test.ts` holds `public/sitemap.xml` to the plugin in `vite.config.ts` that stamps it at build: every entry dated, the rest of the document untouched, and the same result on a second pass. The assertion that matters least on a green day is the one about entries — without one to rewrite the plugin does nothing at all, and the dates go back to being whatever someone last typed. Which day it stamps is the other half: the last commit that touched `src/content`, asked of git by `scripts/contentDate.ts`, and the build's own day only when git cannot say. The build date alone was right whenever a build followed a content change and wrong for every other build, re-dating every page on a code-only deploy as if its words had moved. The same day is defined into the page as `VITE_CONTENT_UPDATED` and written by the footer and the printed CV, so the three cannot disagree; the workflow checks out the whole history (`fetch-depth: 0`) because a shallow clone can only name that commit when it is the checked-out one, and the test that reads a real day off this repository is what keeps the fallback path being the only one that ever ran. The privacy policy's own "Last updated" is deliberately not this day: it is `POLICY_UPDATED`, a typed date of the policy's last wording change, held by `src/components/ui/PrivacyPolicyModal.test.tsx` to not follow the clock — it read `new Date()` before, and told every visitor the policy had changed this month.
- `tests/contentBoundary.test.ts` holds the line the words moved across. Every sentence the site states now lives in `src/content` as JSON, and `src/data/portfolioFacts.ts` is what assembles it into the typed shapes the page and the build read — done so an editor outside this repository can change what the site says by rewriting a file, without a language in the way. That arrangement is worth exactly nothing the day a sentence goes back into the TypeScript, because then the editor shows a page it can only half change and nothing says which half. No type system can see that: a string literal added to the assembly layer compiles, renders, and passes every check this repository had, the 33 kB rendered-content snapshot included — that snapshot is a characterization of the page, and the page did change on purpose. So the property is stated rather than implied. The assembly layer may do two things to content and no others: fill a `{{name}}` placeholder, and join an array of parts into one string. Every string it hands out is checked to be reachable from a content file through one of those two — 253 distinct strings offered, 315 handed out — and anything else is reported as invented. It also fails on a content file the module imports from nowhere, since the import graph walks `.ts` and `.tsx` and a JSON file orphaned in that directory is invisible to it. What is deliberately still code is listed in the module and is not prose: the career start and the years counted from it, which are a computation; the contact details, kept in parts so no source file and no shipped bundle carries a whole address; and the award tone, which is one of three names — `satisfies` catches a fourth added in the code, and a throw catches one added in the JSON, where no type reaches. Its two companions are `src/data/placeholders.ts`, whose substitution throws on a name nothing offers rather than rendering `{{yearsOfExperiance}}` to a visitor and to every crawler until somebody looks, and one line in `tests/module-reachability.test.ts`: moving the portrait's address out of a module and into `src/content/about.json` took it out of reach of a scanner that read only source, and that check reported the picture every visitor sees as an asset nothing points at. The false positive was the cheap version. The expensive one is the next asset named only from content, reported unused and deleted. It covers both assembly modules, because the split was drawn twice: `portfolioFacts.ts` is what the Vite config can load and `portfolioData.tsx` is what needs JSX, and a rule holding for one would leave the other free to state sentences — which is exactly where the 79 the second migration moved had been sitting. What is **not** covered, and is named here rather than left as a gap this pretends to fill: the prose still written into components — section headings, `aria-label`s, the privacy policy's paragraphs, the consent banner's wording. The policy is the interesting one and is deliberately last, since its text has links running through it and putting that in JSON means either markup in content or a parts array, which is a different problem from this one.
- `tests/contentCanonical.test.ts` holds every file in `src/content` to the bytes the content editor writes back. That editor is a separate program: it reads a file through GitHub's Contents API, parses it, changes one field and commits `JSON.stringify(value, null, 2) + "\n"` — so that serialization, and no other, is what a saved file looks like. Three of the sixteen were authored in a style it does not reproduce. `certifications.json` stored every non-ASCII character as a `\uXXXX` escape — `Certified SAFe®`, `Barbara Smoczyńska`, `Wyższa Szkoła Przedsiębiorczości` — where the serializer emits raw UTF-8; `cvLayout.json` and `pageLayout.json` kept their section entries hand-compacted one per line, where it expands every object. Measured before the guard existed: editing one field in `pageLayout.json` rewrote 62 of its 64 lines and `cvLayout.json` 38 of 40, burying the change somebody meant inside a whole-file reformat they did not. Nothing caught it and nothing could, because the *value* never moved — same strings, same key order, same numbers — so `contentShape` was green reading values, `contentReaches` was green reading values, and `tsc` was green because no type changed. The defect lived entirely in the bytes, which is the one thing this repository insists must not change by accident. The three files were normalised in the commit that added this, proven by parsing both sides and comparing including key order, and the guard was proved by putting one back and watching it go red. Two alternatives rejected: teaching the editor to preserve each file's original style, which means modelling escaping and compaction per file and still leaves two writers to disagree the first time a file arrives in a third style — the fix belongs where there is one answer rather than two; and a formatter dependency over JSON, which would be a second description of a rule the serializer already states, to be kept in step with it forever, where this is one expression of that serializer itself.

- `tests/contentUrls.test.ts` holds every value under a `url` key in `src/content` to an `https://` address that parses. A project's link is rendered as an anchor's `href` straight from the content, and nothing held what that string could be: the editor refused blank and padded, and `contentReaches` checked the word arrived. React blocks a `javascript:` address — measured in the security sweep that added this, React 19 renders it as a throwing placeholder — but `http:` ships as a mixed-content link, `data:` as a page the browser may or may not open, and a typo as a link to nowhere. The content is committed by a web app and that commit runs this gate before the deploy, so the rule sits where refusing is cheap; the editor says the same thing in front of the field. By key, not by value, for `contentReaches`' reason: `url` is the one key the page follows, `imageUrl` is a path `tests/portrait.test.ts` already holds, and the CV's `website` and `linkedin` are hosts the template prefixes itself. Proven by putting `javascript:alert(1)` into one project link: red, naming the entry.
- `tests/workflowPins.test.ts` holds every `uses:` in `.github/workflows` to a forty-character commit with the version in a comment beside it, and every workflow to a top-level `permissions` block. A tag is a name its owner — or whoever takes over the account — can move, and `actions/checkout@v4` runs whatever `v4` points at on the day; the security sweep found all four actions pinned that way. Pinning them once lasts until the next `uses:` line is pasted from a README, which is why this is a check and not a commit. `.github/dependabot.yml` is the other half: a pin nobody bumps is stale in the other direction, so Dependabot proposes the bump — the commit and the comment together — and the gate says whether it is safe; the same file watches the npm dependencies, where `npm audit` had a moderate advisory in a dev-only tool the day it was written with nobody to have noticed. Proven by putting one pin back to a tag and by deleting the permissions block, each red.

---

## Run Locally

```bash
git clone https://github.com/rembednarczyk/remigiuszbednarczyk.com.git
cd remigiuszbednarczyk.com
npm install
npm run dev
```

## Running Tests and Quality Checks

```bash
# Development server
npm run dev

# Unit and integration tests
npm run test

# Linter and type check
npm run lint

# Storybook (component driven development and a11y testing)
npm run storybook

# Full quality gate: lint, test, build Storybook, test Storybook, build app
npm run check:quality

# The four checks that need a browser, and run as their own CI steps.
# Each needs `npm run build` first; check:quality ends with one.
npm run check:lighthouse   # scores the built site against the badges above
npm run check:print        # prints the CV to PDF and reads the sheets back
npm run check:motion       # watches the sections arrive, with and without
                           # prefers-reduced-motion
npm run check:focus        # tabs the page and checks every stop shows focus
```

Test coverage spans three layers:

- **Unit and integration** through Vitest: navigation targets resolving to real sections, the scroll spy mapping, data from `portfolioData` reaching the page, and heading structure.
- **Component behaviour** through Storybook interaction tests: focus trapping, keyboard operation, and consent flows.
- **Accessibility** through the Storybook addon, which runs axe on every story and fails the build; the dialogs add a `jest-axe` assertion of their own, since they render through a portal the addon's scan does not reach.

---

## Component Driven Development and Storybook

Storybook serves as an automated accessibility gate. Every reusable component in `src/components/ui/` is required to have a matching `.stories.tsx` file.

Stories are state-driven, covering `Loading`, `MissingFields`, `UnreachableService` and `LongTextOverflow` alongside behavioural cases such as `FocusIsTrapped`, `EscapeRestoresFocus` and `ReopeningAfterAHungSubmission`.

### Edge Case: AI-Assisted Component Usage

The `ProjectCard` stories model how the UI behaves with unpredictable, AI-generated data: very long text, single words with nothing to break on, missing fields, and the same content at 320px. They assert that nothing reaches past the card's own edge, which is what the earlier version of this claimed and never checked — and they run against the card the site actually renders, rather than against components no page used.

Accessibility is checked on every story by the addon, which `.storybook/preview.ts` sets to `test: 'error'` so a violation fails the pipeline rather than appearing in a panel nobody opens. `tests/storyCoverage.test.ts` holds every component in `src/components/ui/` to having one, because a component with no story is a component that scan never sees on its own.

Stories that render through a portal assert it themselves as well:

```tsx
expect(await axe(dialog)).toHaveNoViolations();
```

The addon scans the story's own root, and a dialog is not inside it — so the dialogs name the node to scan.

---

## CI/CD Pipeline and Quality Gate (GitHub Actions)

One workflow, [`ci.yml`](.github/workflows/ci.yml), handles both verification and deployment.

- **Quality gate:** every push and pull request against `main` runs `npm run check:quality`.
- **Strict linting:** custom ESLint rules enforce the Single Responsibility Principle by preventing UI components from importing sections, block animation of layout properties in Motion, and hold the conditions for a 100/100/100/100 Lighthouse score such as `aria-label` presence and correct image loading attributes.
- **Fail-fast:** a failed gate aborts the run before anything reaches production.
- **Continuous deployment:** merges to `main` build and deploy to GitHub Pages. Only the deploy job holds the Pages credentials.
